const mensajes = require('../../bot/mensajes');
module.exports = async (sock, from, text, msg) => {
    //const response = mensajes.comando_desconocido; // Usa el mensaje definido en mensajes.js
    //await sock.sendMessage(from, { text: response });
  };